#!/bin/bash
# This file is only sourced, so variables are used externally
# shellcheck disable=SC2034

# Change these if you want
HOOKS_DIR=/etc/libvirt/hooks
SCRIPTS_DIR=/opt/single-gpu-passthrough

VM_NAME=win10
